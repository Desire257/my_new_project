{% extends 'base.html.twig' %}

{% block title %}Camion{% endblock %}

{% block body %}
    <h1>Camion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ camion.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ camion.nom }}</td>
            </tr>
            <tr>
                <th>Plaque</th>
                <td>{{ camion.plaque }}</td>
            </tr>
            <tr>
                <th>NumeroChassis</th>
                <td>{{ camion.numeroChassis }}</td>
            </tr>
            <tr>
                <th>DateMiseCirc</th>
                <td>{{ camion.DateMiseCirc ? camion.DateMiseCirc|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_camion_index') }}">back to list</a>

    <a href="{{ path('app_camion_edit', {'id': camion.id}) }}">edit</a>

    {{ include('camion/_delete_form.html.twig') }}
{% endblock %}
